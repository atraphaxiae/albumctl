[package]
name = "albumctl"
version = "0.3.0"
authors = ["Nile Jocson"]
edition = "2024"
description = "A declarative builder for your music library."
repository = "https://github.com/atraphaxiae/albumctl"
readme = "./README.md"
license = "MPL-2.0"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
serde = { version = "1.0.229", features = ["derive"] }
